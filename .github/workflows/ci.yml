name: CI
on:
  push:
  pull_request:
jobs:
  docker-build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test via Docker
        run: docker build -t java-debugging-system .
